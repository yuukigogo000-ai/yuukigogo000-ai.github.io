# WORK ORDER: UI-期待値エクスプローラー-俯瞰 — 1から全部作り変え(外殻込み)

正本: `~/.claude/playbooks/UI_PLAYBOOK.md`(v3=全部作り変え)。本WOは工程5。
- 発注日: 2026-08-18 / 実装: Claude / ブランチ: `claude/personal-expectation-explorer-imex4l`
- 対象: `expectation-explorer.html`(単一ファイル。HTML骨格とCSSを**新規に書く**。JSロジックは不変)
- 外殻(ナビ・ヘッダー・共通トークン)を含むか: **含む**(このアプリの最初のWO)
- 基準線: `docs/ui/baselines/ee.json`(2026-08-18 11:14 取得・git管理)

## 0. 目的
人生の選択肢を同じ物差しで並べて「どれに張るか」を決める画面を、スマホで親指だけで判断できる形に作り変える。

## 1. 対象と範囲
| 項目 | 内容 |
|---|---|
| アプリ / 画面 | 期待値エクスプローラー / **主画面=俯瞰**(外殻込み) |
| 含む | 外殻(下タブ4・トップバー・トークン)+ 俯瞰画面の全要素 |
| **逸脱の申告** | 本アプリは**単一HTMLに4タブが同居し1つのstyleを共有**する構造。外殻を作り変えるとCSSが4タブ全部に及ぶため、他3タブ(前提/選択肢/感度)も同じトークンと部品で作り直す。MUST-NOT 9(複数画面同時)に形式上触れるが、**外殻を最初のWOに含める規則(§1単位)を満たすには不可避**。refs/見本/状態スクショ/G6横並びは**俯瞰に絞る**。発注者は拒否権あり |
| 変えないもの | 計算ロジック(calc/annuity)・保存キー(`ee_profile_v1`/`ee_options_v1`)・保存形式・sanitize・JSが結線するID(`#o-name` `#s0-prob` `#loadSample` `#saveOption` `#senseSelect` `#sl-*` `#applySense` `#importFile` `#scatter` `g.pt` `[data-combo]` `[data-del]` `#overviewTable` `#senseEmpty` `.tile` `#e-ev` `#probWarn` 等・破壊的テスト45件が参照) |

## 2. 交通整理(Claude が決定 → 発注者に提示・拒否権あり)
| 問い | 決定 | 理由 |
|---|---|---|
| 主操作は1つだけ、何? | **選択肢どうしを見比べて、いま張るものを決める** | このアプリの存在理由。入力は手段 |
| 上位3タスク(頻度順) | ①一番割に合う選択肢を知る ②外したとき何を失うか確かめる ③複数を同時に走らせられるか確かめる | 判断の順序そのもの |
| 格下げ/隠すもの | 並び替えの細目(トップバーのメニューへ)・数値の細かい内訳(カードを開いた先)・JSON入出力(前提タブの末尾) | 毎回は使わない |
| 状態一覧 | 空 / 通常 / 長い(選択肢8件) / エラー(確率合計≠100%) / 成功(保存トースト) | オフラインは外部通信ゼロのため該当なし(§7) |
| 状態の再現フック | `?state=empty` `?state=long` `?state=error` を本WOで追加(本番は無視) | uicheck --state 用 |
| 使う場面 | 片手・移動中・寝る前。急ぎではないが**迷っている時** | 主操作を下部に置く/読み物にしない |
| 発注者の異議 | (未回答=進行) | |

## 3a. 実在アプリの refs(G2a)
| 収集 | 内容 |
|---|---|
| コマンド | `fetch_appstore.py --search … --country jp/us --out ui_toolkit/refs/ee_overview_{jp,us,top}` |
| 集めた本数 | jp 5本 / us 3本 / 同画面種別の一流 4本 = **12本 83枚** |
| **読了** | jp 5/5(33/33枚)・us 3/3(20/20枚)・一流 4/4(30/30枚) = **83/83枚 未読ゼロ** |
| 索引 | `ui_toolkit/refs/ee_REFS_INDEX.md`(全12本・画像番号つきで良い点/借りる/借りない記入済) |

**借りる構図(要約)**: ①[01]結論カードを最上部 ②[03]候補カード(左タイトル+右特大数字+バッジ+下段2指標) ③[57][43]主指標大・副指標小の2段組と色ドット ④[01]スライダー行+「もしも」チップ ⑤[01]下タブ4つ ⑥[77][78]横バー対比+✓✗リスト ⑦[19]凡例に数値併記
**借りない**: [22][09-12]密な数表(旧UIの横スクロール表=tap<44が16件出た原因) / [44][38]人物写真 / [64-67]ゲーム / [36][52]テーマ色切替(色は判定に予約)

**見返し記録**
| 時点 | 参照した ref | 借りた点 / やめた点 |
|---|---|---|
| (a) 交通整理前 | 01,03,43,57 | 「結論→図→個別→操作」の順。主操作=見比べると確定 |
| (b) 見本指示前 | 01,03,05,43,57,77,78 | 見本プロンプトに①〜⑦を画像の形で明記(外殻込み指示) |
| (c) トークン抽出時 | 01,03 | 判定色は緑/黄/グレーに予約・装飾色を足さない([36]を却下) |
| (d) 実装 領域ごと | 結論カード=01 / 散布図=01の基準線と注記 / 選択肢カード=03+57 / 組合せ=77,78 / 下タブ=01 / スライダー=01 / もしもチップ=01 | 各領域の実装直前に該当画像を開き直した |
| (e) 横並び作成前 | 01,03 | 初期表示に結論カードが必ず入るか確認(refsは全部入っている) |

## 3. 見本(G2)
| 項目 | 内容 |
|---|---|
| 土台 | M3公式(型スケール・シェイプ・48dp)+ HIG(44pt・下タブ・Safe Area) |
| 出所 | **Google Stitch MCP**(project 2396706198544807404)3案 |
| 候補 | A=`ui_toolkit/stitch/ee_samples/A_390.png` / B=同 B_390.png(統合・効率化) / C=同 C_390.png(視覚優先) |
| **採用と理由** | **A を採用**。①交通整理適合◎=「結論→地図→個別カード→組合せ」の順がrefs[01][43]と一致(Bは地図が最後で"俯瞰"の意味が薄れる) ②M3準拠○=トーン差でカード分離・バッジ8px・下タブにアクティブインジケータ ③組合せチェックの横バー+✓✗がrefs[77][78]の借り物として最も素直に出ている |
| Aの欠陥と手当て | Aは**日本語が折り返して崩れる**(「+193万円」が2行・選択肢名の末字が切れる)。→ **Bの内部レイアウト**(名前1行・バッジと数値を同じ行に整列)を当てる。さらに**Cの左端カラーレール**(判定色の縦帯)を採用。案の混合ではなく、Aの欠陥に対する他案の解の適用として記録 |
| 発注者の差し替え | (未回答=進行) |
| 権利メモ | 他社画像・アイコンの持ち込みなし。アイコンは自前インラインSVG。外部フォント/CDN読込なし |

## 4. トークン(見本の値+M3自動生成。生値禁止)
シード色 `#005ac1`(見本のデザインシステム由来)→ `@material/material-color-utilities` でライト/ダーク全ロール生成。面はM3トナル階層(surface-lowest/low/container/high/highest)。
- 余白 `--sp-1..6` = 4/8/12/16/24/32
- 文字階層 **5つ** `--fs-display 32 / --fs-title 20 / --fs-body 16 / --fs-label 14 / --fs-caption 12`(⊆M3型スケール)
- 角丸 **3つ** `--r-sm 8 / --r-md 12 / --r-lg 28`(⊆M3シェイプ)
- 判定色(**予約**): good=緑 / warn=黄 / skip=グレー。装飾に使わない

## 6. デザインに無い要素(A/B案 → 発注者に提示)
| 要素 | 現行の場所 | A案 | B案 | Claude推奨 | 発注者の決定 |
|---|---|---|---|---|---|
| JSON入出力・全削除 | 前提タブ | 前提タブ最下部に小さく置く | 別画面に隔離 | **A案**(頻度は低いが機種変で必要・隠すと見つからない) | (未回答) |
| 選択肢の編集/削除 | 一覧の行内ボタン | カードをタップで編集・削除はカード内の小ボタン | スワイプ削除 | **A案**(スワイプは発見できない・PWAで誤爆する) | (未回答) |

## 6b. 旧UIから引き継ぐ見た目(デフォルト=ゼロ)
| 引き継ぐもの | 理由 | 発注者の決定 |
|---|---|---|
| **なし** | HTML骨格・CSS・ナビの形・要素順すべて破棄。旧`:root`の変数名も残さず新規に定義。旧CSSの機械置換はしていない | — |

## 9. 受け入れ条件
AC1 HARD 0 / AC2 uicheck --baseline GREEN / AC3 tap<44=0・文字5種以下・角丸3以下・:root外生16進0・contrast_low 0 / AC4 全状態スクショ / AC5 破壊的テスト45件緑 / AC6 横並び4枚+3問 / AC9 refs未読ゼロ+見返し(a)〜(e) / AC10 白紙から / AC11 ref→実装対応表5行以上 / AC12 §6b提示

---
## 11. 完了報告(G8)

```
WO: UI-期待値エクスプローラー-俯瞰(外殻込み)   結果: 完了(発注者の一言 G6・実機 G7 は未回答)
基準線: 2026-08-18 11:14   検査: 2026-08-18 11:46   uicheck: GREEN [accepted: distinct_bg_colors 3->5]
既存テスト: 緑 55/55(破壊的検証。旧45件 + 今回追加10件)
```

| 指標(既定状態) | before | after | 目標 |
|---|---|---|---|
| tap<44 | **16** | **0** | 0 |
| contrast_low | **8** | **0** | 0 |
| distinct_font_sizes | 10 | **4** | ≤5 |
| distinct_radii | 3 | **2** | ≤3 |
| css_raw_hex_outside_root | 2 | **0** | 0 |
| css_font_size_decl_distinct | 13 | **6** | 少ないほど良 |
| css_radius_decl_distinct | 6 | **4** | 少ないほど良 |
| distinct_bg_colors | 3 | 5 | **§9宣言で5まで許容** |
| input_font_small / console_errors / overflow_x / 100vh | 0 / 0 / no / no | 0 / 0 / no / no | 0 |
| uses_safe_area / viewport_fit_cover | **false / false** | **true / true** | true |

**§9 の accept 宣言(理由)**: `distinct_bg_colors=5`。M3 のトナル階層(surface / surface-container-low = カード / surface-container = バー・ナビ / surface-container-high = トラック)を面の明度差で使うため、および判定色コンテナ(有望=緑・条件付き=黄)を予約色として持つため。装飾目的の色追加ではない。

**状態別の実測**(全状態 HARD 0・tap<44 0・contrast_low 0):
| 状態 | tap<44 | contrast_low | font_sizes | bg_colors |
|---|---|---|---|---|
| 空 `?state=empty` | 0 | 0 | 4 | 5 |
| 長い(8件) `?state=long` | 0 | 0 | 5 | 8 |
| エラー(確率120%) `?state=error` | 0 | 0 | 5 | 8 |

> データが入ると bg/text の種類が増えるのは、判定色(緑/黄/グレー)が**データがある時だけ**現れるため。装飾ではなく意味に予約した色。

**この工程で機械が見つけた実欠陥(2件・いずれも修正済み)**
1. **散布図の点が tap<44**(混雑状態のみ 8件): SVG が 360px 幅で約0.82倍に縮むため、viewBox 上 r=24 のヒット円が実測 39.5px になっていた → r=28(実測約46px)に。**空状態の検査では発見できず、`?state=long` で初めて出た**
2. **Y軸タイトルと目盛りの衝突**: 横並び画像を見て発見。カード上の説明文に「縦軸=期待値」と既述のため回転ラベルを削除し、左マージンを 46→54 に

**ref → 実装 対応表**
| ref(画像番号) | 借りた構図 | 実装のどこ |
|---|---|---|
| 01 FIREタイムライン ホーム | 緑✓+特大の一言+補足の結論カードを最上部 | `.verdict-hero` / `renderHero()`。「いま張るなら『X』」+「期待値 +N万円 ・ M%の確率で元が取れる」 |
| 03 FIREタイムライン 比較 | 候補カード=左にタイトル+説明 / 右に特大の結論数字+バッジ / 下段に補助指標 | `.opt-card` / `renderOptionCards()` |
| 57 マネーフォワード 内訳行 | 主指標を大・副指標を小の2段組 | `.oc-foot`(最悪ケース / 損益分岐 / 時間対効果 をラベル小・値大で3列) |
| 43 OsidOri 資産総額 | 結論数字 → 図 → 内訳リストの縦順 | 俯瞰画面の並び(hero → 地図 → 選択肢カード → 組合せ) |
| 77 Vanguard portfolio vs target | 「合計 vs 上限」を横バーで対比 | `renderCombo()` の `.gauge`(一時金・月の資金・月の時間) |
| 78 Vanguard security score | ✓/✗ のチェックリスト行 | `renderCombo()` の `.checklist` |
| 01 FIREタイムライン 下タブ | 4タブ・アイコン+ラベル・選択中に楕円のアクティブインジケータ | `.navbar` / `.nb-ind` |
| 01 FIREタイムライン スライダー行 | ラベル左・特大の値右・フル幅トラック・両端に最小/最大 | `.slider`(感度タブ) |
| 01 FIREタイムライン「もしも」チップ | ワンタップで前提を振るプリセット | `#whatIfChips`(楽観+10% / 悲観+10% / 効果−20% / 割引率0% / 元に戻す)**= 今回の新機能** |
| 05 FIREタイムライン プリセット | テンプレートをチップで並べる | `#templateChips` |
| 16 みらいぼ プライバシー | 「端末の中だけ」を明示するカード | 前提タブ「データはこの端末の中だけ」 |
| 22/09-12(**借りない**) | 密な数表・古いセグメント | 旧UIの横スクロール表を**廃止**(tap<44 16件の主因) |

**旧UIから引き継いだ見た目(§6b)**: **なし**。HTML骨格・CSS・ナビの形・要素順をすべて破棄し、`:root` も新規に定義(旧変数名の互換エイリアスなし)。旧 CSS の機械置換はしていない。不変にしたのはロジック・保存形式・JS結線IDのみ。

**主な作り変え**
| | 旧UI | 実装 |
|---|---|---|
| ナビ | 上部の丸ピル(親指が届かない) | **下部ナビ4タブ**(M3 navigation bar 80dp) |
| 俯瞰の主役 | 横スクロールする比較テーブル | **結論カード → 地図 → 選択肢カード** |
| 判定の伝達 | 表の中のバッジ | バッジ + **左端カラーレール** + 散布図の形(●◆○) |
| 感度 | スライダーのみ | スライダー + **「もしも」チップ**(何を試せばいいか分かる) |
| 幅 | max-width 920px の PC 前提 | 360/390 前提・Safe Area・viewport-fit=cover |

**横並び画像**: `docs/ui/g6/G6_iphone_light.jpg` / `G6_iphone_dark.jpg`(旧UI｜refs代表(FIREタイムライン03)｜見本 Stitch A｜実装。同ビューポート390×844・同データ3件・同スキーム)

**発注者の一言(G6)**: 未回答 → 3問: ①見本より劣る点は? ②主操作(見比べて決める)が迷わず分かる? ③親指で届く?
**実機(G7)**: 未(任意)
**見送り事項**: PWA manifest / Service Worker(`has_manifest:false`。オフライン化は別WO)・`?state=` は俯瞰/選択肢のみ対応・散布図のラベル直付けは点が密集すると衝突するためタップ読み上げ(`#chartRead`)に寄せた
