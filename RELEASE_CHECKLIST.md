# 公開チェックリスト(汎用)

**どのプロジェクトにも使える形で書いてある。** Web・デスクトップ・ゲーム・CLI・ライブラリ、技術スタックを問わない。
新しいプロジェクトを始めるときは、このファイルをそのプロジェクトにコピーして使う。

> このファイルはリポジトリに置いてある(`~/.claude/playbooks/` はマシンによって存在しないため)。
> ローカル機で全プロジェクト共通にしたいときは `~/.claude/playbooks/` にコピーする。

---

## 使い方

1. **規模を選ぶ**(下の「規模で使い分ける」)
2. 該当する段の項目を上から埋める
3. **「必須」がすべて ✅ になるまで公開しない**
4. **当てはまらない項目は空欄にせず「該当なし + 理由」を書く。** 空欄は「まだ見ていない」と区別がつかない
5. ✅ にできない項目が残ったまま公開するなら、**その理由を「未検証の残リスク」に書いてから**公開する(黙って飛ばさない)

チェックの付け方の原則:**「やったはず」ではなく「確かめた」だけを ✅ にする。** 確かめ方の欄に書いてある手段を実際に踏むこと。

---

## 規模で使い分ける

チェックが重すぎると、小さな修正のときに丸ごと飛ばされる。飛ばされないように3段階にする。

| 段 | 使うとき | やること |
|---|---|---|
| **S(小)** | 文言修正・軽微なバグ直し・見た目の微調整 | A2 / B1 / B4 / C1 / C2 の5つだけ |
| **M(中)** | 機能追加・既存機能の作り替え | S + A(全部) + B + C + E1・E2 |
| **L(大)** | 新規公開・大改修・UI全面刷新・データ形式の変更 | 全項目 |

迷ったら重いほうを選ぶ。**Sで済ませたのに利用者のデータが消えた**、が一番高くつく。

---

## A. 中身(そもそも良いものになっているか)

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| A1 | 設計書(完成系の定義)と実物が一致している | 設計書がズレると、次に直すとき何が正しいか分からなくなる | 設計書を上から読み、実物と違う箇所を直す | ● |
| A2 | **主役の機能が、最初から最後まで1回通る** | ここが通らないものは公開してはいけない | 実際に通しで1回やる | ● |
| A3 | 深さの検査6項目に答えられる(数値の根拠/境界/相互作用/反復耐性/熟練者の視点/主役機能の解像度) | 「動く」と「通用する」は別 | 答えられない項目が残っていないか確認 | ● |
| A4 | テストプレイ3回の記録があり、出た指摘が処理済み(直した/理由つきで見送った) | 使ってみて初めて分かる欠陥は、使わないと出ない | `PLAYTEST_LOG.md` を見る | ● |
| A5 | 入れないと決めたものが、理由つきで書いてある | 後で同じ検討を繰り返さないため。レビューの誤検知も防げる | 設計書の「入れないと決めたもの」を見る | |
| A6 | 前の版より良くなったことを数値で言える | 感想ではなく事実で判断するため | タップ数・所要時間・項目数などの前後比較 | |

## B. 壊れなさ

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| B1 | **テストが存在し、リポジトリにコミットされている** | 手元で流して捨てたテストは、次に直すとき存在しないのと同じ | リポジトリ内にテストファイルがあるか | ● |
| B2 | テストが実際に落ちることを確認した(変異注入) | 「絶対に落ちないテスト」は緑でも無意味 | わざと壊してテストが落ちるか見る。**変異検査はコミットしてから** | ● |
| B3 | テストが「いま作ったもの」を見ている | 古いビルド・別ツリー・止め忘れたサーバを検査して緑になる事故がある | ビルド結果と検査対象が同一か照合する仕組みを入れる | ● |
| B4 | 自動検査(CI)が緑 | 人間の記憶に頼らないため | 該当ブランチの CI の結果 | ● |
| B5 | 破壊的検証が PASS(指摘は修正、見送りは理由と出典つきで記録) | 作った本人には見えない欠陥を出すため | レビュー記録を見る | ● |
| B6 | エラー時に操作不能になる経路がない | 詰まると利用者は二度と戻ってこない | 想定エラーを実際に起こす(通信断・権限拒否・不正な入力・容量超過) | ● |
| B7 | **旧バージョンの保存データが読める**(移行がある場合は移行が動く) | 利用者のデータを壊すのは取り返しがつかない | 旧形式のデータを用意して起動する | ● |
| B8 | 連打・二重起動・途中中断で壊れない | 実利用では必ず起きる | 荒く扱って確認 | |
| B9 | 大量データ・長期間使用で破綻しない | 使い込むほど壊れるものは信用を失う | 上限付近のデータ量で試す | |

## C. 公開物

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| C1 | **ビルド出力が最新**(ソースと一致している) | ソースを直して再ビルドを忘れると、公開版だけ古いまま | 再ビルドして差分が出ないか | ● |
| C2 | **公開先で実際に開いて動く**(手元でなく本番の場所で) | 手元で動くことと公開先で動くことは別 | 本番URL/配布物を開いて主役の機能を1回通す | ● |
| C3 | 更新が利用者に届く | キャッシュ・自動更新の設計を誤ると、直しても古い版が使われ続ける | 前の版を開いた状態から更新されるか確認 | ● |
| C4 | 入口からの導線が通っている(一覧・目次・検索用の記述) | 存在を知られなければ無いのと同じ | トップページ等から実際に辿る | |
| C5 | バージョン表記が上がっている | 「どの版で壊れたか」を後から特定するため | 表示・ファイル名・タグ | |
| C6 | **前の版に戻せる**(戻し方が分かっている) | 公開後に致命的な問題が出たときの唯一の逃げ道 | 戻す手順を書く。手順が無いなら書いてから公開 | ● |

## D. 利用者への説明・法務

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| D1 | 初見の人が「何をするものか」を数秒で分かる | 分からないものは使われない | 説明を読まない人の目で最初の画面を見る | ● |
| D2 | **外部にデータを送るなら、送信先を明記し、ソフト内からリンクする** | 課金の有無と無関係に、公開した時点で必要 | 送信処理を全部洗い出し、記載と突き合わせる | ● |
| D3 | 保存されるデータと、その消し方が書いてある | 利用者が自分で始末できるようにする | 説明を読んで実際に消せるか | ● |
| D4 | 出力の正確性についての断り書きがある | 予報・価格・判定・提案などは外れることがある | 免責の記載 | |
| D5 | 第三者の権利を侵していない(商標・ライセンス・素材の出典) | 後から差し止めになると全部やり直し | 使っている名前・ロゴ・ライブラリ・画像の出所を確認 | ● |
| D6 | 課金するなら、法定の表記と利用規約がある | 無いと販売できない | 該当法令の要求項目 | |
| D7 | 対象外の利用者への配慮(年齢・用途の制限) | 内容によっては必要 | 記載の有無 | |

## E. 実物での確認

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| E1 | **想定する実機・実環境で触った** | 開発環境だけで分かることには限りがある(指の届く位置、文字の大きさ、動作の重さ) | 実際の端末・実際のOSで主役の機能を通す | ● |
| E2 | **本物のデータ・本物の外部サービスで通した**(模擬ではなく) | 模擬応答のテストは「壊れないこと」しか保証しない。**中身の質は保証しない** | 本番と同じ経路で数回使う | ● |
| E3 | 回線が細い・切れる状況での挙動 | 実利用では普通に起きる | 通信を絞る/切って試す | |
| E4 | **初回起動(データが何も無い状態)** | 作った本人の環境にはデータが溜まっていて気づけない | まっさらな状態で起動する | ● |
| E5 | 想定する最小構成(古い端末・小さい画面・低解像度) | 一番弱い環境が利用者の環境かもしれない | 縮小した条件で確認 | |

## F. 公開後に備える

| | 確認すること | なぜ | 確かめ方 | 必須 |
|---|---|---|---|---|
| F1 | **未検証の残リスクが書いてある** | 「確認していないこと」を自分で把握しているかどうかが、次の事故の分かれ目 | 設計書の残リスク欄 | ● |
| F2 | 「壊れた」と言われたときの再現手段がある(版・環境・手順を聞ける) | 情報が無いと直せない | 版の表示があるか、聞くべきことが決まっているか | |
| F3 | 連絡手段がある | 報告を受け取れなければ改善できない | 記載の有無 | |

---

## 判定

- **必須(●)がすべて ✅** → 公開してよい
- **必須に空欄がある** → 公開しない。埋めるか、「該当なし + 理由」を書く
- **必須を1つでも ✅ にできないまま公開する** → その理由を残リスクに明記してから公開する。**黙って飛ばさない**

## 記録の残し方

公開のたびに、このチェックリストを埋めたものを残す。1行でもよい。

```
2026-08-18 v1.5 公開 / 規模: M
- 必須すべて ✅
- 該当なし: D6(課金なし) / E5(対象端末を絞っているため)
- 残リスク: 実APIでの通し確認は3回のみ。長期利用時の挙動は未確認
```

**これを残しておくと、次に事故ったとき「前回は何を見て、何を見ていなかったか」が分かる。** チェックリストの本当の価値はここにある。
