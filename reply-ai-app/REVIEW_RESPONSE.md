FIX_REQUIRED: stale request guards can surface obsolete results
- [SEVERITY critical] src/components/ReplyTab.tsx:192 - the request ID is invalidated only by sample loading, not by editable conversation/profile/goal/tone/image inputs - generate for conversation A, edit to conversation B before the response, and A’s replies render under B and can be copied or added to adopted history.
- [SEVERITY critical] src/components/ProfileTab.tsx:130 - the profile request guard is never invalidated when profile inputs, mode, or images change - editing a user’s facts during generation still displays and allows copying a bio produced from the old facts.
- [SEVERITY major] src/components/ReplyTab.tsx:115 - sample loading invalidates an active request without cancelling or detaching its busy state - selecting a sample during a stalled request clears the result but leaves generation disabled until that obsolete request completes or reaches the 120-second timeout.
- [SEVERITY major] src/components/Dropzone.tsx:40 - concurrent image loads share a single boolean busy flag - if two selections overlap and the first finishes while the second is decoding, busy becomes false and generation can silently omit the second image before its thumbnail appears.
- [SEVERITY minor] tests/e2e.test.js:487 - the PWA update/cache tests do not exercise their stated guarantees - string matching cannot detect an automatic update reload, and the fresh offline context never primes an API response, so an implementation that cached successful API responses could still pass the “API is not cached” assertion.