FIX_REQUIRED: stale replies can be applied to changed inputs
- [SEVERITY critical] src/components/ReplyTab.tsx:168 - Completed results and `lastReplies` are not invalidated when request-defining inputs change, while regenerate blindly reuses that history - generate for conversation A, replace the conversation with B, then click the still-visible regenerate button; A’s replies are inserted into B’s request as prior proposals, and if regeneration fails the old A cards reappear beside B’s inputs where they can be copied to the wrong match.
- [SEVERITY major] src/lib/api.ts:76 - The timeout is cleared and external cancellation listener removed immediately after response headers arrive, before `res.json()` consumes the body - if Anthropic returns headers and the connection stalls while downloading JSON, the 120-second timeout and sample-switch cancellation no longer work, leaving generation and its buttons stuck indefinitely.
- [SEVERITY major] src/components/ReplyTab.tsx:109 - Image fingerprints record only encoded lengths rather than image identity; the same defect exists in `ProfileTab.tsx:93` - while a request for screenshot A is pending, replace it with screenshot B whose resized JPEG has the same byte length; the fingerprint remains unchanged and the response based on A is silently rendered under B’s current input.