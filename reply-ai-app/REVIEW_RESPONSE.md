FIX_REQUIRED: reply generation can silently return stale or incomplete results
- [SEVERITY critical] src/components/ReplyCard.tsx:20 - clipboard success is never awaited or checked before recording adoption - when `writeText` is unavailable or rejects with `NotAllowedError`, the UI reports success and adds an uncopied reply to `reply_ai_adopted`, while the rejected promise is unhandled
- [SEVERITY critical] src/lib/storage.ts:43 - adopted-history write failures are swallowed and the unsaved array is returned as though persistence succeeded - under quota/private-storage failure the UI says the history is active, but a reload silently loses it
- [SEVERITY major] src/components/ReplyTab.tsx:161 - starting a new generation does not clear or associate the existing result with its original inputs - after generating for conversation A, changing to conversation B, and receiving a 401/refusal/network failure, A's cards reappear and can be copied as if they answered B
- [SEVERITY major] src/components/ReplyTab.tsx:126 - a fresh generation clears `lastReplies` before the request succeeds - if that request fails, the old visible result remains but “方向性を変えて別の3案” sends no history, allowing the same old proposals to be returned
- [SEVERITY major] src/lib/prompts.ts:9 - the reply schema has no `minItems`/`maxItems` for `replies` or `bubbles` - a schema-valid response with zero, one, or two proposals, or an empty bubble list, is accepted and rendered instead of enforcing three usable proposals
- [SEVERITY major] src/components/ReplyTab.tsx:104 - loading a sample leaves uploaded images, current results, and regeneration history intact - after uploading a real screenshot and generating, selecting a fictional sample and pressing regenerate sends the old screenshot and old replies together with the sample context
- [SEVERITY major] src/components/ReplyTab.tsx:121 - the validation gate rejects the supported initial-message flow when no conversation screenshot/text exists - in a clean session, selecting sample ④ supplies a partner profile and style sample but clicking generate only shows the missing-conversation error and makes no API request
- [SEVERITY major] src/lib/api.ts:16 - the API request has no timeout or abort mechanism - a stalled connection leaves `fetch` pending indefinitely, so `run` never reaches `finally` and generation buttons remain disabled
- [SEVERITY minor] tests/e2e.test.js:201 - test 16g claims to prove generation from an empty conversation plus profile/style, but five screenshots from test 10 remain attached - the assertion passes through unrelated image state and does not detect the clean-session initial-message failure