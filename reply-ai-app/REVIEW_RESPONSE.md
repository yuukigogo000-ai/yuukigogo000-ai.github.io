FIX_REQUIRED: live input and adopted history can be lost
- [SEVERITY critical] vite.config.ts:13 - `autoUpdate` activates the new service worker and reloads controlled pages without preserving React state - during a slow deploy update, a user can paste a conversation or add screenshots before activation, then lose all of it when the page reloads
- [SEVERITY critical] src/App.tsx:76 - multi-bubble adoption repeatedly reloads history from localStorage instead of carrying the in-memory list forward - when storage writes fail and a two-bubble reply is copied, the first bubble is discarded even though the UI promises session-only history
- [SEVERITY critical] src/lib/prompts.ts:9 - the reply schema has no `minItems`/`maxItems`, and the renderer accepts one or two replies - a schema-valid one-reply response displays only one proposal despite the product promise of three
- [SEVERITY major] tests/e2e.test.js:42 - service workers are blocked and later checks only verify that `sw.js` returns HTTP 200 - a worker that serves stale assets, reloads active forms, or intercepts the Anthropic request still passes the PWA tests
- [SEVERITY major] tests/e2e.test.js:53 - the API mock records only the JSON body and never inspects request headers or URL leakage - deleting `x-api-key`, moving the key into the body/URL, or omitting the direct-browser header leaves the API tests green
- [SEVERITY major] tests/e2e.test.js:171 - the XSS assertion checks only whether event-handler payloads execute, not whether markup was injected - rendering the reply bubble through `innerHTML` inserts an inert `<script>` node while this assertion still passes
- [SEVERITY minor] tests/e2e.test.js:449 - the console assertion suppresses every error containing `Failed to load resource`, not just the intentional network-abort case - unrelated missing or blocked resources are silently accepted by the claimed “no console errors” check