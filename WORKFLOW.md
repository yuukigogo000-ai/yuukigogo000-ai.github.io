# 開発手順書(このリポジトリの標準フロー)

このリポジトリでソフト/アプリを作るときに毎回踏む順序。Replier(`reply-ai-app/`)・セトリズム(`band/`)・パチスロ帝国(`pachinko/`)・honmono はすべてこの流れで作られている。

工程は10。**前の工程が終わっていないのに次へ進まない**のが唯一の重要なルール。特に:

- 中身が固まる前にUIを作らない
- **「動いた」で中身を終わらせない**(工程3・4がこれを潰す担当)
- 破壊的検証を PASS させる前に公開しない

---

## フロー全体

```
0. 相談・研究        → 何が面白いのか / 誰の何を解決するのか
1. 設計書            → DESIGN.md に「完成系」を定義
2. 中身の実装        → ロジック・データ。テストを並走で育てる
3. 参考調査と自己改善 → 実在ソフト・文書・利用者の不満を調べ、足りない深さを埋める
4. テストプレイ3回   → 実際に通しで遊ぶ/使う。1回ごとに直して次へ
5. UI                → 中身が固まってから刷新。素材は指示書を書いて発注
6. 検証の下準備      → ビルド→テスト→変異注入。テストが本当に落ちるか確認
7. 破壊的検証        → Codex を PASS まで反復。指摘と見送りを記録
8. 公開              → GitHub Pages / Actions でリリース
9. 公開後            → 実機・実ユーザー検証 → 法務・集客
```

工程3と4は**「中身の突き詰めが甘い」を構造で防ぐための工程**。ここを飛ばすと、UIは綺麗だが中身が浅いものができる。

---

## 0. 相談・研究

コードを書く前に、作るものの正体を言葉にする。

- **ジャンル研究**:そのジャンルの「何が面白いのか / 何が価値なのか」を分解して書く(例:`pachinko/DESIGN.md` の「シミュレーションゲームの面白さ」「経営ゲームの面白さ」)
- **競合・実データ調査**:必要なら別ファイルに残す(例:`reply-ai-app/RESEARCH.md`, `RESEARCH_LINE_STYLE.md`)
- ここで出た結論が、後の工程で「これは仕様通り」と言い切る根拠になる

## 1. 設計書(DESIGN.md)

**仕様書ではなく「完成系の定義」**として書く。作りながら v2, v3 と更新していい。

含めるもの:

- 完成系の姿(クリア条件/成功条件を数値で)
- 機能表(何を・なぜ入れるのか。「なぜ」の列を必ず持つ)
- 画面構成とユーザーに見えるメッセージ一覧(エラー文言も含めて)
- 開発ロードマップ(Phase 1 / 1.5 / 2 …と状態 ✅ 🔨 🔜)
- 品質の評価軸(何が満たされたら「良い」のか)
- **入れないと決めたもの**(理由つき。工程3で埋まっていく)
- **未検証の残リスク**(空欄で作り、工程が進むごとに埋める)

## 2. 中身の実装

見た目は後回しにして、ロジック・データ・プロンプトを完成させる。

- **テストは並走で育てる**。節目にまとめて書くのではなく、機能を足したらその場で項目を足す(Replier は 58 → 87 → 131 項目と育った)
- 実ブラウザ + API モックの E2E を基本にする(Playwright)
- **テストは書いたら必ずコミットする。** リポジトリに残っていないテストで得た「全通過」は、次に直すときには何の役にも立たない
- ここで「壊れない・取り違えない」挙動を作り込む(競合・古い state・保存失敗・中断)

この工程の終わりは「動いた」ではない。**動いた状態が工程3の入口**。

---

## 3. 参考調査と自己改善 🔍

**目的:「動くもの」を「そのジャンルとして通用するもの」に上げる。**

自分の頭の中だけで作ると、そのジャンルで当然あるべき深さに届かない。実物と文書を見て、足りないものを自分で見つけて埋める工程。

### 3-1. 参考を3系統から集める

| 系統 | 何を見るか | 得られるもの |
|---|---|---|
| **実在ソフト** | 同ジャンルの定番と最新(有料含む)。機能一覧・スクショ・レビュー記事 | 当然あるべき機能、業界の標準的な作り |
| **理論・文書** | ゲームデザイン論、UXガイドライン、規格・仕様、実務記事、学術資料 | なぜそうするのかの根拠、数値の妥当性 |
| **利用者の不満** | ストアの低評価レビュー、掲示板、Q&Aサイト、SNS | **一番効く。既存品が満たせていない需要** |

3系統目を必ずやる。「既存ソフトにある機能」を真似るだけでは既存ソフトを超えない。**既存ソフトの利用者が文句を言っている箇所**が、こちらが勝てる場所。

### 3-2. 比較表を作る

- 参考ソフトが持っていて**自分にないもの**
- 自分が持っていて**参考ソフトにないもの**(=売りになる。守るべきもの)

### 3-3. 不足を3つに分類する(全部作ろうとしない)

- **A:入れる** — 完成系の定義に含まれるべきだった。DESIGN.md を更新して実装する
- **B:入れない** — 理由を書いて DESIGN.md の「入れないと決めたもの」へ。**判断を記録するのが目的**(後で迷わない/Codexにも欠陥報告させない)
- **C:後回し** — ロードマップの Phase 2 以降へ

### 3-4. 深さの検査(突き詰めの甘さを潰す本体)

コア機能を1つずつ取り上げて、次の6つを自問する。**答えられない箇所が、甘い箇所**。

1. **数値の根拠** — そのパラメータはなぜその値か。実データや出典があるか。「なんとなく」で置いた数字はどれか
2. **境界** — 0件のとき / 1件のとき / 最大数のとき / 極端な入力のときどうなるか
3. **相互作用** — 機能Aを使うと機能Bはどう変わるか。単独では動くが**組み合わせると意味を失う**箇所はないか
4. **反復耐性** — 10回・100回使ったらどうか。飽きないか、データが膨れないか、破綻しないか
5. **熟練者の視点** — そのジャンルの上級者が触って最初に文句を言う点はどこか
6. **一番おいしい部分の解像度** — このソフトの主役の機能に、脇役と同じ手間しか掛けていないなら足りない

### 3-5. 記録

- 調べたもの・採ったもの・捨てたもの(理由つき)を `RESEARCH.md` に追記
- 出てきた改善点を優先順位つきで `IMPROVEMENTS.md` に列挙し、**やったものにチェックを入れていく**
- 実装した内容は DESIGN.md に反映(設計書と実物をズラさない)

---

## 4. テストプレイ3回と自己改善 🎮

**目的:自分が作ったものを利用者として使い、使ってみて初めて分かる欠陥を潰す。**

実際にアプリを起動して**通しで3回**使う。**1回ごとに視点を変え、毎回そのラウンドで見つけた分を直してから次のラウンドへ進む**(3回やってから直すのでは効きが弱い)。

### ラウンド1 — 初見の利用者として

- 説明・README を読まずに触る
- **どこで詰まったか / 何が分からなかったか / 何を押せばいいか迷ったか**を記録
- 目的を最後まで達成できるか(達成できなければそれが最優先の欠陥)

### ラウンド2 — 想定利用者として本気で使う

- 実データ・実シナリオで最後まで通す
  - セトリズム → 本番1日分(セトリ作成 → 練習 → 本番モード → 終演まとめ)
  - パチスロ帝国 → 30日経営を完走
  - Replier → 実際の会話3件で返信を作り、採用まで
- **途中で萎える箇所・作業が面倒な箇所・惜しいと感じた箇所**を記録
- 目的は達成できるか。達成できても「これなら手作業の方が早い」になっていないか

### ラウンド3 — 荒く扱う / 上級者として

- 連打・戻る・途中離脱・リロード・大量データ・最短ルート探し・想定外の順番
- **壊れ方**を記録(工程7の破壊的検証の下ごしらえにもなる)
- **システムを理解した後でも面白い/使えるか**(ゲームなら最適解1つで終わらないか、ツールなら2回目以降が速くなるか)

### 各ラウンドで必ず記録すること

| 項目 | 例 |
|---|---|
| やったこと | セトリを新規作成して10曲入れた |
| 期待 | 曲を選べば長さが自動で足される |
| 実際 | 長さ未入力の曲があると合計が黙って短くなる |
| 気持ち | 気づかず本番で足りなくなる。怖い |
| 数値 | 目的達成まで **42タップ / 3分20秒 / 迷い4回** |

**数値を残すのが大事**。「使いにくい」は直せないが「42タップ」は減らせる。次のラウンドで同じ数値を測れば、改善したかが分かる。

### やり方(実行手段)

- ブラウザアプリは Playwright + Chromium で実際に操作し、**各画面のスクショを撮って見る**
- Electron 版は `npm start`
- 起動方法が分からないときは `/run` を使う

### 終わったら

- 3ラウンド分を `PLAYTEST_LOG.md` に残す(ラウンド / 発見 / 直したか / 数値の変化)
- 直さないと決めたものは理由を書く
- **UI刷新(工程5)の後に、ラウンド2だけもう1回やる。** 見た目を変えると操作の流れが変わり、中身は無傷でも使い勝手が壊れることがある

### 正直な限界

私がやるテストプレイでは次は分からない。だから工程9の実機・実ユーザー検証は省略できない。

- **実機の触り心地**(指の届く位置、タップの取りこぼし、スマホでの文字の大きさ)
- **本当に面白いか / 本当に嬉しいか**の感覚
- 長期間(数週間)使ったときの飽き

---

## 5. UI

中身が固まってから着手する。MVP を作り替えることを恐れない(Replier は単一 HTML → React+TS+Vite+Tailwind+Radix に全面刷新した)。

- **素材は指示書を書いてから発注する**:`ASSET_BRIEF.md` / `IMAGE_BRIEF.md` に外部AI向けの制作指示を書く
- 見た目の試作は `ui-workbench/` で単独確認できる
- PWA 化(manifest / service worker / アイコン)、ダークモードもこの工程
- **終わったら工程4のラウンド2をもう1回**通す

## 6. 検証の下準備(過去の事故から生まれた必須手順)

破壊的検証に入る前に、テストが機能していることを保証する。

1. **`npm test` の前に必ずビルドする**(`"pretest": "npm run build"`)
   - 事故:変異を入れたら TS エラー → ビルド失敗 → 古いバンドルを検査してテストが緑になった
2. **重要な修正には変異注入して、テストが実際に落ちることを確認する**
   - 変異は「コンパイルが通る形」で入れる(型エラーで止まると検査にならない)
3. **変異検査は必ずコミットしてから行う**
   - 事故:未コミットの修正を `git checkout src` で自分で巻き戻した
4. 文字列(`\n` など)を含むパッチは Write ツールで書く
   - 事故:Windows の bash ヒアドキュメントがバックスラッシュを潰し、テストの `\n` が実改行になった

## 7. 破壊的検証(Codex)

`codex exec -s read-only` に**プロジェクト専用プロンプト**を渡し、**PASS が出るまでラウンドを回す**。

- 専用プロンプトを作る理由:共有プロンプトは「外部API呼び出し=即重大」という物差しなので、外部APIが本体機能のアプリでは使えない
- プロンプトに必ず入れる節(雛形は `reply-ai-app/CODEX_REPLIER_PROMPT.txt`):
  - `APPROVED DESIGN` — 承認済みの設計判断(これを欠陥として報告させない。**工程3-3のB=入れないと決めたものもここに書く**)
  - `WHAT COUNTS AS CRITICAL` — このアプリで致命的なもの
  - `WHAT TO IGNORE` — 様式・命名・「バックエンドを付けろ」など
  - `METHOD` — 端から端まで追わせるフロー(入力→組み立て→送信→描画→保存→次の入力)
  - `OUTPUT FORMAT` — 1行目を `PASS` か `FIX_REQUIRED: <理由>` に固定
- **指摘は全部直さない。見送りは理由と出典をつけて記録する**
- 記録の残し方:
  - `REVIEW_REQUEST.md` — 何をしたか / 特に見てほしい点 / スコープ外
  - `REVIEW_LOG.md` — ラウンド別の指摘表、見送り(理由つき)、**自分が起こした事故と再発防止**
- 実績:Replier は r1〜r6 で 34件 → 修正31 / 見送り3、r6 で PASS

## 8. 公開

- Web:GitHub Pages(ソースと `npm run build` の出力を分ける。例 `reply-ai-app/` → `reply-ai/`)
- デスクトップ:GitHub Actions の Desktop Build(手動実行で Artifacts / `desktop-v1.0.0` 形式のタグ push で Release に自動添付)
- `sitemap.xml` / `robots.txt` / ルート `index.html` からの導線も更新する

## 9. 公開後

公開はゴールではない。DESIGN.md のロードマップに沿って続ける。

- **実機確認**(iPhone / Android のタッチ操作・PWA インストール)
- **実ユーザー検証**(品質の評価軸で測る)
- バックエンド・認証・課金が必要なら Phase 2
- **特商法・規約・プライバシーポリシー**、SEO・集客は Phase 2.5
- DESIGN.md の「未検証の残リスク」を最新に保つ(消すのではなく、検証できたら結果を書く)

---

## 節目ごとにやること(まとめ)

| タイミング | やること |
|---|---|
| 機能を足したとき | E2E に項目を足す。**テストをコミットする** |
| 中身が動いたとき | **工程3**:参考3系統を調べて比較表 → 深さの検査6項目 → 実装 |
| 工程3が済んだとき | **工程4**:テストプレイ3回(視点を変え、毎回直して次へ) |
| 大きな修正のあと | コミット → 変異注入 → テストが落ちるか確認 |
| UI 刷新の完了時 | テストプレイ ラウンド2 を再走 → ビルド → テスト全通過 → Codex 開始 |
| Codex ラウンドごと | 指摘を修正 or 理由つき見送り → REVIEW_LOG.md に追記 |
| 公開の直前 | PASS を確認 / 残リスクを DESIGN.md に明記 |
| 公開の直後 | 実機で触る / 設計書と説明書を実物に合わせて更新 |

## 各工程で残るファイル

| ファイル | いつ書くか |
|---|---|
| `DESIGN.md` | 工程1で作り、以降ずっと更新 |
| `RESEARCH.md` | 工程0・3(調べたこと) |
| `IMPROVEMENTS.md` | 工程3(改善点リストとチェック) |
| `PLAYTEST_LOG.md` | 工程4(3ラウンドの記録と数値) |
| `ASSET_BRIEF.md` / `IMAGE_BRIEF.md` | 工程5(素材の発注書) |
| `CODEX_*_PROMPT.txt` | 工程7(検証用プロンプト) |
| `REVIEW_REQUEST.md` / `REVIEW_LOG.md` | 工程7(依頼と結果) |
