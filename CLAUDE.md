# このリポジトリでの作業ルール

## 開発フロー

新しいソフト/アプリを作るとき、既存プロジェクトに大きな変更を入れるときは **[WORKFLOW.md](WORKFLOW.md) の10工程に従う**。

要点だけ:

1. **「動いた」で中身を終わらせない。** 中身が動いたら必ず工程3・4を通す
   - **工程3**:実在ソフト・理論文書・**既存品への利用者の不満**の3系統を調べ、比較表を作り、深さの検査6項目(数値の根拠/境界/相互作用/反復耐性/熟練者の視点/主役機能の解像度)で甘い箇所を埋める
   - **工程4**:実際に通しで**3回テストプレイ**する(初見 → 本気 → 荒く扱う)。**1回ごとに直してから次のラウンドへ。** タップ数・所要時間・迷った回数を数値で残す
2. **中身が固まる前にUIを作らない**(UI刷新後はテストプレイ ラウンド2 を再走)
3. **破壊的検証(Codex)を PASS させる前に公開しない**
4. **テストは節目にまとめて書かず、機能を足した時点で足す。書いたら必ずコミットする**(残っていないテストの「全通過」は次に直すとき役に立たない)
5. **変異検査は必ずコミットしてから**。`npm test` の前にビルドが走ることを確認する(`pretest`)
6. **Codex の指摘は全部直さない**。見送りは理由と出典をつけて `REVIEW_LOG.md` に残す

## プロジェクト一覧

| パス | 内容 |
|---|---|
| `band/` | セトリズム(バンド用セトリ設計 PWA・Vanilla JS) |
| `desktop/` | セトリズムの Electron 版 |
| `reply-ai-app/` | Replier(返信コーチ)のソース。React+TS+Vite+Tailwind+Radix |
| `reply-ai/` | 上記の `npm run build` 出力(GitHub Pages 配信用・手書き禁止) |
| `pachinko/` | パチスロ帝国(ホール経営シミュレーション) |
| `honmono/` | AI検出・クリエイター向けツール群 |
| `ui-workbench/` | UI 試作の置き場 |
| `index.html` | JAN→楽天価格 取得ツール(個人用) |
| `privacy.html` | サイト共通のプライバシーポリシー。各アプリからリンク済み |
| `.github/workflows/ci.yml` | push のたびに走る検査(ビルド・E2E・公開物の鮮度・構文) |

## 注意

- ビルド出力ディレクトリ(`reply-ai/` など)を直接編集しない。ソース側を直して再ビルドする
- **`reply-ai-app/` を直したら必ず `npm run build` して出力もコミットする**(忘れると公開サイトは古い版のまま。CI が止める)
- **データの送信先を増やしたら `privacy.html` を更新する**
- E2E を回すときはリポジトリのルートを配信するサーバが要る。**別のクローンのサーバが同じポートを掴んでいると古い版を検査してしまう**ため、テスト先頭の番人が一致を確認する(違えば即停止)
- 各プロジェクトの設計書 `DESIGN.md` が完成系の定義。実装を変えたら設計書も合わせて更新する
